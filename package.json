{
  "name": "",
  "description": "",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "start": "node src",
    "lint": "eslint ./src --fix",
    "prettier": "prettier ./src/**/*.* --write"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.js": [
      "npm run prettier",
      "npm run lint",
      "git add"
    ]
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.17",
    "eslint": "^6.7.2",
    "husky": "^3.1.0",
    "jest": "^24.9.0",
    "lint-staged": "^9.5.0",
    "prettier": "^1.19.1"
  }
}
